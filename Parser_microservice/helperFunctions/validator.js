// recieve an array of urls and return links not found in database.
const dbManager = require("./dBConnection");

const validateUrlsNotVisited = (url) => {

  const visitedUrl = dbManager.getOne()

  if (condition) {
  } else {
    return;
  }
};

if (validateUrl()) {
}

module.exports = validateUrlsNotVisited;
